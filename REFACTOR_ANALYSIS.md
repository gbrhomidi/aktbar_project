# تحليل إعادة الهندسة

## نقطة الانطلاق

المستودع الحالي هو تطبيق PWA ثابت وليس مشروع Android. لا توجد وحدات `app/` أو ملفات Gradle أو `AndroidManifest.xml` أو Gradle Wrapper. نقطة الدخول هي `index.html`، وتُحمّل ملفات JavaScript عامة من مجلد `js/` وموارد CSS/assets.

## المعمارية الحالية

الواجهة عبارة عن صفحة HTML واحدة كبيرة مع قائمة جانبية ونوافذ/شاشات يتم إظهارها وإخفاؤها عبر DOM. ملف `js/ui.js` يجمع بين التنقل، تحديث الواجهة، استدعاء قاعدة البيانات، CRUD للأسئلة، الاستيراد والتصدير، الإعدادات، الشبكة، والنتائج. ملف `js/game.js` يضم منطق جلسة الاختبار مع العرض والمؤقت والتقييم داخل DOM.

طبقة البيانات الحالية تعتمد على Dexie.js فوق IndexedDB في `js/db.js`، مع fallback ذاكرِي. الجداول الحالية هي الأسئلة، الإعدادات، التصنيفات، الإنجازات، XP، سجل الألعاب، سجل المراجعة، وإحصاءات اللاعب. توجد أيضًا `localStorage` عبر `js/store.js` و`js/theme-manager.js`.

الاتصال بين الأجهزة يعتمد على WebRTC مع QR signaling داخل `js/network.js`. لا توجد في المستودع الحالي مؤشرات على NanoHTTPD أو localhost أو `127.0.0.1` أو Local Server أو REST API داخلي. الـ Service Worker هو آلية PWA مستقلة، وقائمة الأصول فيه قديمة وتشير إلى ملفات UI غير موجودة.

## قرار التحويل

سيُضاف مشروع Android Native قابل للبناء باستخدام Kotlin وJetpack Compose وNavigation Compose. ستصبح طبقة Native هي الواجهة الأساسية لمسارات المنزل وإدارة الأسئلة والاختبار والإعدادات والإنجازات والشبكة، بينما يُحافظ على HTML القديم داخل WebView مستقل عبر شاشة LegacyWebView عند الحاجة. لن يُستخدم WebView كـ backend.

سيُستبدل IndexedDB بطبقة SQLite مباشرة عبر `DatabaseHelper.kt`، مع Repository فعلي لكل مجال رئيسي، وViewModel يدير الحالة والـ loading/error/empty states. ستُحافظ وظائف النسخ الاحتياطي عبر `BackupWorker`، وتُقدّم وظيفة SMS عبر `SMSService` مع صلاحيات Android المناسبة. ستبقى أصول PWA داخل `src/main/assets/legacy` للاستخدام الانتقالي داخل WebView.

## مخاطر معروفة

المشروع لا يحتوي على تطبيق Android سابق يمكن نقله حرفيًا، لذلك ستكون النتيجة إعادة بناء Native مع الحفاظ على الأصول والواجهة القديمة كمسار Hybrid. وظائف WebRTC وQR المتقدمة ستبقى متاحة في مسار Legacy WebView في هذه المرحلة، بينما يوفّر الغلاف Native تنقلًا وحالة وتخزينًا أصليًا قابلًا للبناء.
