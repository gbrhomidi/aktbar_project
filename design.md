# خطة تصميم واجهة Akeer14 Mobile Telegram Agent

## هدف التجربة

يعمل الهاتف كعامل مراقبة واحد. تُدخل الأسرار والإعدادات محليًا من التطبيق، بينما تظهر لوحة التحكم الفعلية — الأزرار والقوائم المتداخلة ولوحات الإعدادات — داخل محادثة Telegram. يقدّم التطبيق واجهة عربية من اليمين إلى اليسار، موجّهة للاستخدام بيد واحدة في وضع عمودي بنسبة 9:16، وبأسلوب iOS/Android هادئ وواضح: بطاقات كبيرة، حالات تشغيل قابلة للقراءة، وأفعال رئيسية قرب الإبهام.

## قائمة الشاشات

| الشاشة | المحتوى والوظائف |
|---|---|
| **لوحة العامل** | حالة Telegram، الكاميرا، الميكروفون، الشبكة، الخلفية، وآخر أمر ونتيجته. تشمل أزرار تشغيل العامل، إيقافه، فتح اختبار Telegram، وفتح الإعدادات. |
| **إعدادات Telegram** | Bot Token وChat ID وUser IDs المصرح بها، مع تخزين آمن محليًا واختبار `getMe` وإرسال لوحة Telegram حقيقية. |
| **إعدادات المراقبة** | الكاميرا الأمامية/الخلفية، الفلاش، جودة الفيديو والصوت، مدة الفيديو الطارئ، التقريب، الضغط، الحذف، والاحتفاظ بالوسائط. |
| **الكشف والتخزين** | الحساسية، إجراءات الحركة والصوت، إدارة مساحة الأدلة، الضغط والتنظيف. |
| **تشخيص التنفيذ** | سجل مفسر لآخر callback ونتيجة Telegram وحالة الإرسال، مع أخطاء يمكن نسخها أو مراجعتها. |
| **الشروط والخصوصية** | بيان واضح للصلاحيات والوسائط والاتصالات الخارجية وحدود التشغيل في الخلفية. |

## تدفق المستخدم الرئيس

1. يفتح المستخدم التطبيق على الهاتف العامل، يمنح صلاحيات الكاميرا والميكروفون والإشعارات، ثم يدخل Bot Token وChat ID في شاشة الإعدادات.
2. يضغط **اختبار Telegram**؛ يتحقق التطبيق من البوت ويرسل لوحة تحتوي على callbacks فعلية إلى المحادثة المحددة.
3. يضغط المستخدم **تشغيل العامل** من الواجهة المرئية. تظهر حالة العامل وسجل التشخيص داخل التطبيق.
4. من Telegram يرسل المستخدم `/start` أو يضغط أحد أزرار اللوحة. ينفذ العامل الالتقاط أو التسجيل أو تعديل الإعدادات، ثم يرسل النتيجة إلى المحادثة نفسها.
5. من لوحة Telegram يفتح المستخدم Canvas الإعدادات، يغيّر الكاميرا أو الجودة أو التقريب أو الحساسية، ويتلقى تحديث حالة يعكس القيمة المحفوظة فعليًا.

## تدفق الطوارئ

1. يضغط المستخدم **الطوارئ** في Telegram.
2. تظهر لوحة عمليات حقيقية للصورة والصوت والفيديو القصير أو الطويل والبث إطارًا بإطار والإيقاف.
3. يعرض Telegram حالة البدء، ثم النتيجة أو خطأ النقل بالتفصيل. لا تظهر أزرار ديكورية أو عمليات محاكاة.

## قواعد التصميم المرئي

يعتمد التصميم لونًا رئيسيًا أزرق Telegram داكنًا `#1F6FA9`، وخلفية فاتحة `#F5F8FC`، وبطاقات بيضاء `#FFFFFF`. تستخدم حالة الجاهزية الأخضر `#16826A`، والتحذير الكهرماني `#C77A12`، والخطأ الأحمر `#C83E4D`. في الوضع الداكن تكون الخلفية `#0B1620`، والأسطح `#132534`، والنص `#EAF3FA`، مع الحفاظ على التباين. تستخدم العناصر القابلة للنقر ارتفاعًا لا يقل عن 44 نقطة وتباعدًا سخياً، وتبقى الأفعال الخطرة في أسفل البطاقة مع تأكيد نصي.

## متطلب تقني صريح

> وجود الأزرار داخل Telegram يتطلب Bot API فعليًا. أما الاستماع المستمر للأوامر وتنفيذ الكاميرا أو الميكروفون في الخلفية على Android فيتطلب خدمة foreground أصلية أو بنية وسيطة متوافقة مع سياسات النظام؛ لا يكفي تشغيل JavaScript في الخلفية وحده. ستبقى الواجهة والتشخيص صادقين بشأن هذه الحدود، ولن يتم تمثيل تشغيل غير متاح على أنه حقيقي.
